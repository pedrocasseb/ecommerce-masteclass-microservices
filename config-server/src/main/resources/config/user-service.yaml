spring:
  mongodb:
    uri: mongodb://localhost:27017/user-service-db
    database: user-service-db
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: "*"